require 'sinatra'
require 'json'
require 'sinatra/json'<% @state_transitions.each do |state_transition| %>
require_relative 'affordances/<%= "#{state_transition.verb}_#{state_transition.target}" %>'<% end %>

<% @state_transitions.each do |state_transition| %>
<%= state_transition.verb %> '<%= state_transition.url.path %>' do
  json(<%= "#{state_transition.verb.capitalize}#{state_transition.target.capitalize}" %>.new(params).response)
end
<% end %>
